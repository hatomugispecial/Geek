あなたは、優秀なシステムエンジニア兼、ウェブデザイナーです。
飲食店のオーダーシステムをシステム化するプロジェクトについてシステムを構築することになりました。
ただし、スマホで表示することを前提としてスマホでの環境（430px）を考慮してUIを構築してください。
以下の要件を含めてシステムを構築してください。
ただし、このディレクトにはすでにある程度のwebサイトの基盤が揃っているので、最初にこのディレクトリの中身と構造を把握してください。

システム要件：

ディレクトリ構造：
お客さんが座った机に対して、各テーブルに1つ、QRコードを置く。
そのQRコードを読み込むことで、お客さんは注文用のwebシステムを使う事ができる。（Geekディレクトリ内にOrderディレクトリを作成し、そこでOrderシステムを管理する）
また、店側にも受けた注文を管理するシステムが存在し、geekディレクトリ内に管理する。（Storeディレクトリとする）
そして、Orderシステムで受けた注文をStoreシステムに反映するためのBackエンドのディレクトリ（Backendディレクトリ）を使用する。

お店のコンセプト：
クライアント: 「株式会社 OSAKI ダイニング」（架空）
所在地: 東京都品川区大崎
事業: 「OSAKI 亭」などの複数ブランドの飲食店を全国展開しています。 直営店が 20 店舗、フランチャイズ加盟店が 80 店舗あり、オペレーションを共通化しています。

まずは、注文システムの画面の基本構成を実装します。 特に、店舗客が利用する注文画面を考えます。
店舗側のStoreシステムと、Backendのシステムはまだ作らなくて良いです。

注文画面のデザインは以下のようにしてください。
1.ヘッダーに店舗名があり、その下に料理の各カテゴリーごとにメニューのページが表示されるようにしてください。
（カテゴリーの例：おつまみ、おかず、サラダ、カクテル、日本酒、ウイスキー、ビール、その他アルコール）
2.各メニューの写真（まだ入れなくて良い）を押すと、そのメニューをいくつ注文するかが表示されるようにしてください（ポップアップ）。数量の選択肢としては、0から4まで選べるようにしてください。（メニューが押された場合のデフォルトの注文数は1にしてください。）そして、"追加"ボタンを押すと、そのメニューが数量分、"現在のカート"に追加されるようにしてください。
3.一番下には、固定のバーがあるようにしてください。
    固定のバーには、左はじから、"今までの注文履歴"、"現在のカート"、"注文する"というボタンがあるようにしてください。
4."今までの注文履歴"では、今までに注文されたメニュー名とその個数、値段が書かれた行が縦に並び、一番したに固定で総額が書かれるようにしてください。
5."注文する"では、"現在のカート"の中身を表示し、一緒に表示される"注文を確定"というボタンを押すと、店舗側と"今までの注文履歴"の部分に追加されるようにしてください。

次に、OrderのUIをもう少し改善していきます。以下の内容に合わせてコードを修正してください。
1.メニューのカテゴリーの一番左に"すべて"というカテゴリーを追加して、そのカテゴリー内には全てのメニューがカテゴリーごとに表示されるようにしてください。
2.各料理の価格をもう少し大きく表示してください。
3.各メニューの写真を適当に生成してメニューに貼り付けてください。

---------------------ここまで反映済み

"今までの注文履歴"の合計金額に対して、ユーザーが入力した人数で割り勘したときの 1 人あたりの支払金額を表示する「割り勘機能」を実装してください。
品切れのメニューを"現在のカート"に追加できず、エラーメッセージを表示する機能を実装してください。

---------------------ここまで反映済み（割り勘・品切れ）

ここまでは Next.js などのフレームワークによるアプリケーション実装により、システムの表面的な箇所を実装してきました。 しかし、実際のウェブ注文システムを動作させるためには、アプリケーションの外に可変なデータを保持するデータベースが必要です。

Vercel にデプロイした Next.js アプリケーションのサーバーサイドから、Neon の PostgreSQL に接続するよう構成してください。
Neon の PostgreSQL にサンプルデータを格納し、それを Next.js 側で取得し、表示してください。

タスクのゴール（終了条件）：
Vercel と Neon を接続していること
Neon の PostgreSQL にサンプルデータを格納していること
Next.js から Neon の PostgreSQL に接続し、サンプルデータを表示できること

---------------------ここまで反映済み（Neon / `DATABASE_URL` / `/neon-sample` は `orders`・`order_items` 表示）

ウェブ API として注文 API を Next.js の Route Handler 等を用いて実装し、必要なデータを受け取ってデータベース等に保存する処理を実装してください。
注文確定ボタンを実装し、新たに作ったウェブ API エンドポイントに対するリクエストをクリック時送信するよう実装してください。

タスクのゴール（終了条件）

ウェブ API として注文 API が実装されていること
注文確定ボタンの処理が実装されていること

このタスクでは、画面上の注文内容をサーバーへ送って保存できるようにします。
見た目だけの注文リストではなく、実際に送信できる状態にするのが目的です。

進め方
注文 API（POST）を実装し、入力バリデーションを入れる。
DB 保存処理を実装（注文ヘッダ + 明細の構造を意識）。
注文確定ボタンから API を呼び出す。
成功/失敗時の UI フィードバックを表示。

最低限の品質ポイント
不正なリクエストを保存しない
二重送信対策（送信中ボタン無効化など）
保存成功時にユーザーが結果を認識できる

---------------------ここまで反映済み（注文 API `POST /api/orders`・Neon 保存・送信中無効化・成功表示）

店舗の従業員が受信した注文を確認するための注文受信画面の UI を実装しましょう。

受信した注文一覧を表示してください。
注文のステータス（未着手、調理中、提供待ち、提供済み、取消済み）を操作できるようにしてください。
店舗客の会計時に、座席番号から注文の総量を確認できる機能を実装してください。

タスクのゴール（終了条件）

受信注文一覧が実装されていること
注文ステータスが操作できること
座席番号から注文の総量を確認できること

このタスクでは、店舗の従業員が注文を確認しやすい画面を作ります。
どの注文が来ていて、今どの状態なのかが見やすいことを意識してください。

進め方
注文一覧テーブル（またはカード）を作る。
ステータス変更 UI（セレクト / ボタン群）を実装。
座席番号で絞り込み、会計用の総量（件数・金額）を表示。
操作後に一覧へ即反映（楽観更新 or 再取得）。

チェック項目
ステータス変更が保存される
座席番号単位で集計できる
取消済みなどの状態が判別しやすい

---------------------ここまで反映済み（店舗 `/store` 受信一覧・ステータス更新・座席会計集計・API）

店舗側の注文のステータスの表示を日本語にしてください
お客さんの注文画面で、最初に座席番号を入力できるようにしてください。
そして、座席ごとに注文が管理できるようにしてください
店舗のダッシュボードで注文を管理するときには、料理ごとに行を設けてください
お客さんの座席番号ごとに注文ページが独立して存在し、それぞれの注文が干渉しないようにしてください
"注文する"が成功したときには、"注文が正常に送信されました"と出力して、失敗したときにはエラー処理をしてください。

---------------------ここまで反映済み（店舗ステータス日本語・`/Order`→`/Order/[seat]`・席別ストレージ・店舗は料理行テーブル・成功文言）

店舗客はログイン不要で注文画面を利用できるようにしつつ、従業員向けの画面と機能はログインしないとアクセスできないように制御する必要があります。

Better Auth などのライブラリを利用し、ログイン機能とページ、API ごとのアクセス制限を実装してください。

タスクのゴール（終了条件）

ログイン機能が実装されていること
店舗客はログイン不要で注文画面を利用できること
従業員向け画面と機能はログイン時のみ利用できること

進め方
認証ライブラリ（例: Better Auth）を導入。
ログインページとセッション管理を実装。
従業員向けページにガードを設定。
API 側にも認可チェックを実装（UI ガードだけにしない）。
顧客向け注文画面は未ログインでもアクセス可能にする。

最低限の確認
未ログインで従業員画面へ入れない
ログイン後は従業員機能が利用できる
顧客向け画面はログイン不要で使える

---------------------ここまで反映済み（Better Auth・`/login`・`/register`・`proxy.ts` で `/store` と `/api/store/**` を保護・店舗 API は `requireStaffSession`）